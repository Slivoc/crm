document.addEventListener('DOMContentLoaded', function() {
  const root = document.querySelector('[data-customer-id]');
  if (!root || !root.dataset.customerId) return;
  const customerId = root.dataset.customerId;
  const canEdit = root.dataset.canEdit === 'true';
  const state = { selected: [], suggestions: [], perplexitySuggestions: [] };
  const endpoint = `/customers/${customerId}/deep-dive`;
  const perplexityEndpoint = `/customers/${customerId}/deep-dive/perplexity-suggestions`;
  const escapeHtml = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money = value => `£${Number(value || 0).toLocaleString(undefined, {maximumFractionDigits: 0})}`;
  async function readJson(response) { const data = await response.json().catch(() => ({})); if (!response.ok || data.success === false) throw new Error(data.error || `Request failed (${response.status})`); return data; }
  function reason(row) { const parts = []; if (row.shared_tag_count) parts.push(`${row.shared_tag_count} shared tags`); if (row.shared_industry_count) parts.push(`${row.shared_industry_count} industries`); if (row.shared_company_type_count) parts.push(`${row.shared_company_type_count} types`); if (row.order_count) parts.push(`${row.order_count} orders (${money(row.total_order_value)})`); if (row.country) parts.push(row.country); return parts.join(' • ') || 'Similar profile'; }
  function key(row) { return `${row.related_customer_id || row.id}:${row.relationship_type || row.suggested_type || 'potential'}`; }
  function addCustomer(row, type) { const item = { related_customer_id: row.related_customer_id || row.id, name: row.name, country: row.country, website: row.website, status: row.status || row.status_name, assigned_salesperson_name: row.assigned_salesperson_name, order_count: row.order_count || 0, total_order_value: row.total_order_value || 0, last_order_date: row.last_order_date, relationship_type: type, notes: row.notes || '' }; if (!state.selected.some(existing => key(existing) === key(item))) state.selected.push(item); renderSelected(); }
  function addMatchedSuggestion(index) { const row = state.perplexitySuggestions[index]; if (!row?.crm_match) return; addCustomer({...row.crm_match, notes: row.why || row.similarity_basis || '', order_count: 0, total_order_value: 0}, 'potential'); }
  function removeCustomer(id, type) { state.selected = state.selected.filter(row => !(String(row.related_customer_id) === String(id) && row.relationship_type === type)); renderSelected(); }
  function updateNotes(id, type, notes) { const item = state.selected.find(row => String(row.related_customer_id) === String(id) && row.relationship_type === type); if (item) item.notes = notes; }
  function selectedHtml(row) { const id = row.related_customer_id; return `<tr><td style="min-width:220px"><a href="/customers/${id}/edit" target="_blank" class="fw-semibold text-decoration-none">${escapeHtml(row.name)}</a><div class="small text-muted">${escapeHtml(row.country || '')} ${row.status ? '• ' + escapeHtml(row.status) : ''} ${row.assigned_salesperson_name ? '• ' + escapeHtml(row.assigned_salesperson_name) : ''}</div></td><td class="small text-muted">${row.order_count || 0} orders<br>${money(row.total_order_value)} lifetime</td><td><textarea class="form-control form-control-sm deep-dive-note" rows="2" data-id="${id}" data-type="${row.relationship_type}" placeholder="Positioning notes..." ${canEdit ? '' : 'disabled'}>${escapeHtml(row.notes || '')}</textarea></td><td class="text-end"><button type="button" class="btn btn-sm btn-outline-danger deep-dive-remove" data-id="${id}" data-type="${row.relationship_type}" ${canEdit ? '' : 'disabled'}><i class="bi bi-x"></i></button></td></tr>`; }
  function renderSelected() { const potential = state.selected.filter(row => row.relationship_type === 'potential'); const existing = state.selected.filter(row => row.relationship_type === 'existing'); document.getElementById('deepDivePotentialBody').innerHTML = potential.length ? potential.map(selectedHtml).join('') : '<tr><td class="text-muted p-3">No potential targets saved yet.</td></tr>'; document.getElementById('deepDiveExistingBody').innerHTML = existing.length ? existing.map(selectedHtml).join('') : '<tr><td class="text-muted p-3">No benchmark customers saved yet.</td></tr>'; }
  function renderSuggestions() { document.getElementById('deepDiveSuggestionCount').textContent = state.suggestions.length; document.getElementById('deepDiveSuggestionsBody').innerHTML = state.suggestions.length ? state.suggestions.map(row => `<tr><td><a href="/customers/${row.id}/edit" target="_blank" class="text-decoration-none fw-semibold">${escapeHtml(row.name)}</a><div class="small text-muted">Score ${row.similarity_score || 0} • ${escapeHtml(row.country || 'Country unknown')}</div></td><td class="small text-muted">${escapeHtml(reason(row))}</td><td class="text-end text-nowrap"><button type="button" class="btn btn-sm btn-outline-primary deep-dive-add" data-id="${row.id}" data-type="existing" ${canEdit ? '' : 'disabled'}>Use as context</button></td></tr>`).join('') : '<tr><td colspan="3" class="text-muted p-3">No context customers found.</td></tr>'; }
  function renderPerplexity() {
    const body = document.getElementById('deepDivePerplexityBody');
    body.innerHTML = state.perplexitySuggestions.length ? state.perplexitySuggestions.map((row, index) => {
      const company = row.website ? `<a href="${escapeHtml(row.website)}" target="_blank" rel="noopener" class="fw-semibold text-decoration-none">${escapeHtml(row.name)}</a>` : `<span class="fw-semibold">${escapeHtml(row.name)}</span>`;
      const match = row.crm_match ? `<div class="mb-2"><a href="/customers/${row.crm_match.id}/edit" target="_blank" class="badge text-bg-success text-decoration-none">${escapeHtml(row.crm_match.name)}</a><div class="small text-muted">${escapeHtml(row.crm_match.reason || 'Matched in CRM')}</div></div><button type="button" class="btn btn-sm btn-outline-primary deep-dive-add-matched" data-index="${index}" ${canEdit ? '' : 'disabled'}>Add as target</button>` : '<span class="badge text-bg-light">Not in CRM yet</span>';
      return `<tr><td style="min-width:220px">${company}<div class="small text-muted">${escapeHtml(row.country || '')}</div></td><td class="small text-muted" style="min-width:360px">${escapeHtml(row.why || row.similarity_basis || 'Similar operating profile')}</td><td style="min-width:260px">${match}<div class="input-group input-group-sm mt-2"><input class="form-control deep-dive-match-query" data-index="${index}" placeholder="Search CRM to match"><button class="btn btn-outline-secondary deep-dive-create-customer" data-index="${index}" ${canEdit ? '' : 'disabled'}>Create new</button></div><div class="deep-dive-match-results small mt-1" data-index="${index}"></div></td></tr>`;
    }).join('') : '<tr><td colspan="3" class="text-muted p-3">Click Ask Perplexity to research companies. Then match each result to an existing CRM customer or create a new customer.</td></tr>';
  }
  async function generatePerplexity() { const button = document.getElementById('generatePerplexityDeepDiveSuggestions'); button.disabled = true; button.innerHTML = '<span class="spinner-border spinner-border-sm me-1"></span> Asking...'; document.getElementById('deepDivePerplexityBody').innerHTML = '<tr><td colspan="3" class="text-muted p-3">Perplexity is researching companies and checking CRM matches...</td></tr>'; try { const data = await readJson(await fetch(perplexityEndpoint, {method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json','X-Requested-With':'XMLHttpRequest'}, body:JSON.stringify({count:8})})); state.perplexitySuggestions = data.suggestions || []; renderPerplexity(); } finally { button.disabled = false; button.innerHTML = '<i class="bi bi-stars me-1"></i> Ask Perplexity'; } }
  async function searchMatch(input) { const q = input.value.trim(); const box = root.querySelector(`.deep-dive-match-results[data-index="${input.dataset.index}"]`); if (q.length < 2) { box.innerHTML = ''; return; } const rows = await readJson(await fetch(`/customers/search?q=${encodeURIComponent(q)}&limit=8`, {headers:{'Accept':'application/json'}})); box.innerHTML = rows.length ? rows.map(customer => `<button type="button" class="btn btn-link btn-sm p-0 me-2 deep-dive-use-match" data-index="${input.dataset.index}" data-id="${customer.id}" data-name="${escapeHtml(customer.name)}" data-country="${escapeHtml(customer.country || '')}" data-website="${escapeHtml(customer.website || '')}">${escapeHtml(customer.name)}</button>`).join('') : '<span class="text-muted">No CRM customers found.</span>'; }
  async function createCustomer(index) { const row = state.perplexitySuggestions[index]; const data = await readJson(await fetch(`/customers/${customerId}/deep-dive/create-suggested-customer`, {method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json','X-Requested-With':'XMLHttpRequest'}, body:JSON.stringify(row)})); row.crm_match = data.customer; addCustomer({...data.customer, notes: row.why || row.similarity_basis || ''}, 'potential'); renderPerplexity(); renderSelected(); }
  async function load() { const data = await readJson(await fetch(endpoint, {headers:{'Accept':'application/json','X-Requested-With':'XMLHttpRequest'}})); document.getElementById('customerDeepDiveTitle').value = data.deepdive.title || ''; document.getElementById('customerDeepDiveOverview').value = data.deepdive.overview || ''; document.getElementById('customerDeepDiveStrategy').value = data.deepdive.strategy_notes || ''; state.selected = data.selected || []; state.suggestions = data.suggestions || []; state.perplexitySuggestions = data.deepdive.perplexity_suggestions || []; renderSelected(); renderSuggestions(); renderPerplexity(); }
  async function save() { document.querySelectorAll('.deep-dive-note').forEach(input => updateNotes(input.dataset.id, input.dataset.type, input.value)); const data = await readJson(await fetch(endpoint, {method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json','X-Requested-With':'XMLHttpRequest'}, body:JSON.stringify({title:document.getElementById('customerDeepDiveTitle').value, overview:document.getElementById('customerDeepDiveOverview').value, strategy_notes:document.getElementById('customerDeepDiveStrategy').value, selected:state.selected, perplexity_suggestions:state.perplexitySuggestions})})); state.selected = data.selected || []; state.suggestions = data.suggestions || []; document.getElementById('customerDeepDiveAlert').innerHTML = '<div class="alert alert-success py-2">Customer deep dive document saved.</div>'; renderSelected(); renderSuggestions(); }
  root.addEventListener('click', event => { const add = event.target.closest('.deep-dive-add'); if (add) { const row = state.suggestions.find(item => String(item.id) === String(add.dataset.id)); if (row) addCustomer(row, add.dataset.type); } const remove = event.target.closest('.deep-dive-remove'); if (remove) removeCustomer(remove.dataset.id, remove.dataset.type); const addMatched = event.target.closest('.deep-dive-add-matched'); if (addMatched) addMatchedSuggestion(Number(addMatched.dataset.index)); const useMatch = event.target.closest('.deep-dive-use-match'); if (useMatch) { const row = state.perplexitySuggestions[Number(useMatch.dataset.index)]; row.crm_match = {id: useMatch.dataset.id, name: useMatch.dataset.name, country: useMatch.dataset.country, website: useMatch.dataset.website, reason: 'Manually matched'}; addMatchedSuggestion(Number(useMatch.dataset.index)); renderPerplexity(); } const create = event.target.closest('.deep-dive-create-customer'); if (create) createCustomer(Number(create.dataset.index)).catch(error => { document.getElementById('customerDeepDiveAlert').innerHTML = `<div class="alert alert-danger py-2">${escapeHtml(error.message)}</div>`; }); });
  root.addEventListener('input', event => { if (event.target.classList.contains('deep-dive-note')) updateNotes(event.target.dataset.id, event.target.dataset.type, event.target.value); if (event.target.classList.contains('deep-dive-match-query')) { clearTimeout(event.target._timer); event.target._timer = setTimeout(() => searchMatch(event.target).catch(console.error), 250); } });
  document.getElementById('saveCustomerDeepDive')?.addEventListener('click', () => save().catch(error => { document.getElementById('customerDeepDiveAlert').innerHTML = `<div class="alert alert-danger py-2">${escapeHtml(error.message)}</div>`; }));
  document.getElementById('generatePerplexityDeepDiveSuggestions')?.addEventListener('click', () => generatePerplexity().catch(error => { document.getElementById('customerDeepDiveAlert').innerHTML = `<div class="alert alert-danger py-2">${escapeHtml(error.message)}</div>`; renderPerplexity(); }));
  load().catch(error => { document.getElementById('customerDeepDiveAlert').innerHTML = `<div class="alert alert-danger py-2">${escapeHtml(error.message)}</div>`; });
});
